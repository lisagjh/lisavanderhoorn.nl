<script>
  document.querySelectorAll("a").forEach((link) => {
    link.addEventListener("mouseover", () => {
      link.classList.add("hovered");
    });
  });
</script>

<header>
  <nav>
    <ul>
      <li>
        <a href="#about">About</a>
      </li>
      <li>
        <a href="#work">Work</a>
      </li>
      <li>
        <a href="#blog">Blog</a>
      </li>
      <li>
        <a href="#contact">Contact</a>
      </li>
    </ul>
  </nav>
</header>

<style>
  header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    justify-self: center;
    background: linear-gradient(0deg, transparent 0%, var(--white) 90%);
    box-shadow: 0 1px 4px rgba(0, 0, 0, 0.1);
    backdrop-filter: blur(1px);
    -webkit-backdrop-filter: blur(5px);
    position: sticky;
    top: 0;
    width: 100vw;
    max-width: 75rem;
    z-index: 50;
    border-radius: 0 0 12px 12px;
  }

  nav {
    width: 100%;
    max-width: 65rem;
  }

  ul {
    display: flex;
    flex-direction: row;
    gap: 1rem;
    padding: 1.5rem;
    width: 100%;
  }

  li {
    list-style: "";
  }

  a {
    position: relative;
    text-decoration: none;
  }

  a::before {
    content: "";
    position: absolute;
    bottom: -0.1rem;
    height: 2px;
    width: 0%;
    background-color: var(--primary);
    opacity: 0;
    transition: 0.25s ease-in-out;
    view-transition-name: active-link;
  }

  a {
    position: relative; /* Ensure the parent has positioning */
  }

  /* Create a persistent pseudo-element that's hidden by default */
  a::before {
    content: "";
    position: absolute;
    bottom: -0.1rem;
    height: 2px;
    width: 0%; /* Start with width 0 */
    background-color: var(--primary);
    opacity: 0;
    transition: 0.25s ease-in-out;
    view-transition-name: active-link;
  }

  /* Show and animate on hover */
  a:hover::before {
    width: 100%;
    opacity: 1;
    animation: show-link 0.25s ease-in-out forwards;
  }

  /* This selector only applies after the element has been hovered at least once */
  a.hovered:not(:hover)::before {
    animation: hide-link 0.25s ease-in-out forwards;
  }

  @keyframes show-link {
    from {
      opacity: 0;
      width: 0%;
    }
    to {
      opacity: 1;
      width: 100%;
    }
  }

  @keyframes hide-link {
    from {
      opacity: 1;
      width: 100%;
    }
    to {
      opacity: 0;
      width: 0%;
    }
  }
</style>
