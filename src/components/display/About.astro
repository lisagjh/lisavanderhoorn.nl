---
const quotes = [
  {
    author: "Ischa Gast",
    quote: "Accessibility is love for others",
  },
  {
    author: "Robert R. Coveyou",
    quote:
      "The generation of random numbers is too important to be left to chance",
  },
];
const randomIndex = Math.floor(Math.random() * quotes.length);
const quote = quotes[randomIndex];
---

<section class="about" id="about">
  <div>
    <h2 class="pill">About Me</h2>
    <p>
      Hi! I'm Lisa, a frontend developer who recently graduated from
      <strong>the</strong> frontend degree in the Netherlands,
      <a href="https://fdnd.nl/">FDND</a>
    </p>
  </div>

  <div class="more">
    <h2 class="pill">More About Me</h2>
    <p>
      I enjoy building accessible and user-friendly websites. I have experience
      with writing semantic HTML, modern CSS, JS, and a few JS frameworks.
    </p>
  </div>

  <div class="quote">
    <blockquote>
      <p>{quote.quote}</p>
      <cite>{quote.author}</cite>
    </blockquote>
  </div>
</section>

<style>
  section {
    display: flex;
    flex-direction: column;
    gap: 2rem;
    padding: 2rem;
    width: 100%;
    max-width: 65rem;
  }

  div {
    display: flex;
    flex-direction: column;
    gap: 1rem;
    width: 100%;
    padding: 1.5rem;
    border-radius: 12px;
  }

  div {
    background-color: var(--secondary-light);
  }

  div.more {
    background-color: var(--primary-light);
  }

  span {
    font-weight: 500;
  }

  a.pill {
    align-self: flex-end;
  }

  @media (width >= 34.5rem) {
    section {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 2rem;
      padding: 2rem;
      width: 100%;
      max-width: 65rem;
    }

    div {
      grid-column: 1 / 3;
    }
    div.more {
      grid-column: 2/4;
    }

    div:has(blockquote) {
      grid-column: 1/4;
      width: 70%;
      align-self: center;
      justify-self: center;
    }
  }

  div:has(blockquote) {
    background-color: transparent;
    align-items: center;

    & blockquote {
      border-radius: 0 12px 12px 0;
      width: fit-content;

      & cite {
        font-size: 1rem;
      }
    }

    & blockquote::before {
      color: var(--tertiary-dark);
    }
  }

  @supports (animation-timeline: view()) {
    div {
      animation-timeline: view();
      animation-name: fade-in;
      animation-fill-mode: forwards;
      animation-range: 0% 60%;
    }

    @keyframes fade-in {
      from {
        transform: translateY(10rem);
        opacity: 0;
      }
      to {
        transform: translateY(0);
        opacity: 1;
      }
    }
  }
</style>
