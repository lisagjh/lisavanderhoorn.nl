<section class="about" id="about">
  <div>
    <h2 class="pill">About Me</h2>
    <p>
      Hi, I'm Lisa — a frontend developer who loves crafting accessible,
      visually engaging web experiences.
    </p>
  </div>

  <div>
    <h2 class="pill">More About Me</h2>
    <p>
      I love combining technology and creativity — whether it’s refining UX/UI
      designs in Figma, crafting smooth animations in CSS, or experimenting with
      P5.js.
    </p>
    <p>
      Outside of coding, you’ll usually find me curled up with a book or
      unwinding with a game.
    </p>

    <!-- <a href="/#" class="pill">Learn More</a> -->
  </div>
</section>

<style>
  section {
    display: flex;
    flex-direction: column;
    gap: 2rem;
    padding: 2rem;
    width: 100%;
    max-width: 65rem;
  }

  div {
    display: flex;
    flex-direction: column;
    gap: 1rem;
    width: 100%;
    padding: 1.5rem;
    border-radius: 12px;
  }

  div:first-of-type {
    background-color: var(--secondary-light);
  }

  div:last-of-type {
    background-color: var(--primary-light);
  }

  span {
    font-weight: 500;
  }

  a.pill {
    align-self: flex-end;
  }

  @media (width >= 34.5rem) {
    section {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 2rem;
      padding: 2rem;
      width: 100%;
      max-width: 65rem;
    }

    div:first-of-type {
      grid-column: 1 / 3;
    }
    div:last-of-type {
      grid-column: 2/4;
    }
  }

  @supports (animation-timeline: view()) {
    div {
      animation-timeline: view();
      animation-name: fade-in;
      animation-duration: 1s;
      animation-fill-mode: forwards;
      animation-range: 0% 60%;
    }

    @keyframes fade-in {
      from {
        transform: translateY(10rem);
        opacity: 0;
      }
      to {
        transform: translateY(0);
        opacity: 1;
      }
    }
  }
</style>
